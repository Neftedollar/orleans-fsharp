# Recipes

**Task-oriented paths through the current functional API.**

> **Looking for the first tutorial?** Start with [Getting Started](getting-started.md). This page
> assumes you already know how `grainContract`, `grainFor`, and `FunctionalGrain.ref` fit
> together and sends you directly to the task you need.

## Start a new current-API project

Install the published Orleans.FSharp 5.0.0 template:

```bash
dotnet new install Orleans.FSharp.Templates::5.0.0
dotnet new orleans-fsharp -n MyApp
```

For projects generated by the 5.0.0 template, apply the
[one-line startup correction](release-status.md#the-500-project-template-needs-a-one-line-contract-correction)
before running the silo.

For contributor work against repository changes, clone the repository and install
`./orleans-fsharp/templates` instead. See [Release and Production Status](release-status.md) for
the stable release and its production boundaries.

## Choose the grain shape

| Need | Start here |
|---|---|
| Ephemeral state and typed operations | [Contracts and handlers](functional-grains.md#one-operation-one-argument) |
| Durable state | [Persistence model](functional-grains.md#persistence-model) |
| Events as the source of truth | [Event Sourcing](event-sourcing.md) |
| Read-only operation | [`handleQuery`](functional-grains.md#reply-only-handlers-handlequery) |
| Push to a client | [Functional observers](functional-grains.md#push-to-clients-functional-observers) |
| A sequence returned by one call | [Server-Streaming Replies](streaming-replies.md) |
| Pub/sub between producers and consumers | [Streaming](streaming.md) |
| Cross-grain ACID work | [Transactions](functional-grains.md#distributed-acid-transactions) |
| Call the actor from C# | [Calling from C#](calling-from-csharp.md) |

## Configure the host

| Task | Guide |
|---|---|
| Local silo | [Getting Started](getting-started.md#step-4-configure-the-silo) |
| Storage, stream, reminder, or clustering provider | [Silo Configuration](silo-configuration.md) |
| Standalone client | [Client Configuration](client-configuration.md) |
| Orleans Dashboard | [Dashboard](dashboard.md) |
| TLS, call filters, and secret handling | [Security](security.md) |
| Retry, timeout, and circuit breaker | [Resilience](resilience.md) |

## Evolve a deployed application

Treat these as separate compatibility gates:

1. Route old and new callers with [contract versioning](functional-grains.md#operation-rename-and-contract-version).
2. Keep stored state readable with [versioned state and upcasters](serialization.md#versioned-state-and-upcasters).
3. Keep journal entries and snapshots readable with
   [versioned events and snapshots](event-sourcing.md#versioned-events-and-snapshots).
4. Prove N/N+1 and rollback with separate processes; see
   [Testing rolling updates](testing.md#testing-rolling-updates-and-durable-schemas).

The [Orleans compatibility](compatibility.md) page records the framework versions exercised by CI
and calls out new Orleans capabilities which are not yet wrapped.

## Verify the application

- Unit-test pure transition functions directly.
- Use a real `TestingHost` activation for persistence, serialization, reminders, streams,
  transactions, and lifecycle behavior.
- Keep bytes written by released serializers as fixtures when durable schemas evolve.
- Run the closest [repository example](examples.md) before copying a provider-specific setup.

See [Testing](testing.md) for complete patterns and [Examples](examples.md) for runnable projects.

Migration-only material for earlier authoring models is isolated in the unsupported
[Legacy archive](legacy/index.md).
