---
title: Orleans.FSharp
description: Idiomatic F# API for Microsoft Orleans — computation expressions, not boilerplate
template: splash
hero:
  tagline: Build Orleans grains in idiomatic F#. User-authored API records, immutable state transitions, and zero C# stubs — with transactions, event sourcing, and streaming replies as first-class operations. 2,500+ tests.
  image:
    html: |
      <img src="orleans-fsharp-logo.svg" width="120" height="120" alt="Orleans.FSharp logo" />
  actions:
    - text: Get Started
      link: /orleans-fsharp/getting-started/
      icon: rocket
      variant: primary
    - text: View on GitHub
      link: https://github.com/Neftedollar/orleans-fsharp
      icon: github
---

import { Card, CardGrid, Badge, LinkCard } from '@astrojs/starlight/components';

## Why Orleans.FSharp

Microsoft Orleans is one of the strongest virtual actor runtimes on .NET. But in raw C# style, F# teams often end up with mutable state classes, attribute noise, and repetitive infrastructure code.

Orleans.FSharp keeps full Orleans power while giving you a functional-first authoring model: a grain's public surface is a plain F# record of functions, its behavior is a set of pure `state -> reply` handlers, and there is no interface, attribute, or code-generation bridge to write.

## Before & After

### C# Orleans from F#

```fsharp
type CounterGrain() =
    inherit Grain<int>()

    override this.OnActivateAsync(ct) =
        this.State <- 0
        Task.CompletedTask

    member this.Increment() =
        this.State <- this.State + 1
        this.WriteStateAsync()
```

### Orleans.FSharp — the functional grain runtime

```fsharp
type CounterActor = private CounterActor of unit

[<NoEquality; NoComparison>]
type CounterApi =
    { increment: unit -> Task<int>
      value: unit -> Task<int> }

let contract =
    grainContract<CounterActor, string, CounterApi> () {
        grainType "counter"
        version 1
        stringKey
        readOnly (_.value)
    }

let counter =
    grainFor contract {
        defaultState (fun () -> 0)
        handle (_.increment) (fun _ctx n () -> task { return n + 1, n + 1 })
        handle (_.value)     (fun _ctx n () -> task { return n, n })
    }
```

And the call site is the record itself — no proxy interface, no cast:

```fsharp
let api = FunctionalGrain.ref contract factory "my-counter"
let! n = api.increment ()
```

State transitions are explicit values, the compiler checks the whole surface, and storage is written only when you say so.

:::note[Older authoring models]
The original `grain { }` computation expression still compiles and runs, but its public surface
carries `[<Obsolete>]` (warning, not error) — see
[Functional Grain Runtime](/orleans-fsharp/functional-grains/) for the before/after mapping of
every deprecated entry point. The classic `eventSourcedGrain { }` model is likewise superseded by
the functional `journaledGrainFor` and kept documented. The `siloConfig { }` and `clientConfig { }`
hosting CEs are current and unaffected.
:::

## Features

<CardGrid>
  <Card title="Functional grain runtime" icon="puzzle">
    The current authoring model: API record contracts, explicit key codecs, immutable state, and a fixed wire protocol with precompiled proxies — no C# CodeGen interfaces anywhere.
  </Card>
  <Card title="Full Orleans parity, functionally" icon="zap">
    Distributed ACID transactions, event sourcing over Orleans' own log-consistency providers, implicit stream subscriptions, `IAsyncEnumerable` streaming replies, reentrancy policies, and version-tolerant contracts — all as contract or definition operations.
  </Card>
  <Card title="siloConfig { } CE" icon="gear">
    Configure clustering, storage, streaming, TLS, health checks, and runtime services without extension-method boilerplate.
  </Card>
  <Card title="FSharpBinaryCodec" icon="file-code">
    Serialize F# DUs, records, options, lists, and maps with minimal ceremony and predictable behavior for mixed cluster scenarios.
  </Card>
  <Card title="2,500+ Tests" icon="check-circle">
    2,500+ unit and integration tests on a dual-version Orleans CI matrix, including property-based verification with FsCheck and a 16-feature live tour.
  </Card>
  <Card title="Callable from C#" icon="seti:c-sharp">
    A typed facade over any functional contract: ordinary awaited calls and `await foreach` over streaming operations, generated at runtime with no build step.
  </Card>
</CardGrid>

## By the Numbers

| Metric | Value |
|--------|-------|
| Test suite | **2,500+** unit + integration tests |
| Orleans support | **10.1.0 – 10.2.2**, CI-tested at both ends of the range |
| Feature tour | **16** Orleans capabilities demonstrated live in `examples/feature-tour` |
| NuGet packages | **7** — core, runtime, abstractions, testing, event sourcing, analyzers, templates |
| License | **MIT** |
| Local API overhead | **~8ns** in benchmark scenarios |

## Quick Start

```bash
dotnet new install Orleans.FSharp.Templates
dotnet new orleans-fsharp -n MyApp
cd MyApp
dotnet build && dotnet test
```

A working Orleans.FSharp solution with a counter grain, FsCheck property tests, and silo configuration — ready in under 2 minutes.

## Packages

<CardGrid>
  <Card title="Orleans.FSharp">
    Core developer surface: the functional grain runtime (`grainContract`/`grainFor`/`journaledGrainFor`), observers, streaming, logging, reminders, timers, serialization, and the deprecated `grain {}` CE.
  </Card>
  <Card title="Orleans.FSharp.Runtime">
    Hosting and client runtime: `AddFunctionalGrain` registration, `siloConfig {}` and `clientConfig {}` builders for production setup.
  </Card>
  <Card title="Orleans.FSharp.Abstractions">
    The fixed functional transport: request envelopes, protocol tokens, and precompiled Orleans proxies — reference it once, generate nothing.
  </Card>
  <Card title="Orleans.FSharp.EventSourcing">
    The classic `eventSourcedGrain { }` model and its runtime bridge, kept for existing code; new event-sourced grains use `journaledGrainFor` from the core package.
  </Card>
  <Card title="Orleans.FSharp.Testing">
    TestHarness, WebTestHarness, MockGrainFactory, GrainArbitrary, and log capture utilities.
  </Card>
  <Card title="Orleans.FSharp.Analyzers">
    Compile-time safety checks (for example OF0001 for `async { }`) with explicit opt-out where needed.
  </Card>
</CardGrid>

## Supported Orleans Features

Every major Orleans capability, wrapped in idiomatic F#:

- **Grain lifecycle**: activate, deactivate, per-stage `onLifecycle` hooks
- **State persistence**: Memory, Redis, Azure Blob/Table, Cosmos, DynamoDB, ADO.NET
- **Streaming**: Memory, Event Hubs, Azure Queue, broadcast channels — plus implicit subscriptions (`onStream` / `onBroadcast`) that activate a functional grain on publish
- **Streaming replies**: API record fields returning `IAsyncEnumerable<'T>`, riding Orleans' own extension — `await foreach`-able from C#
- **Reentrancy & concurrency**: `reentrant` and per-request `mayInterleave` as contract operations; `statelessWorker` as a definition operation
- **Placement strategies**: `placement` (PreferLocal, ActivationCountBased, ResourceOptimized, Random) as a definition operation
- **Event sourcing**: `journaledGrainFor` — pure `apply` fold over Orleans' `LogStorage`/`StateStorage` log-consistency providers (classic `eventSourcedGrain { }` kept)
- **Transactions**: distributed ACID via `transactional` contract operations and `transactionalStateFrom` facets
- **Contract versioning**: exact by default; opt-in tolerance with `acceptsVersions` and per-operation `sinceVersion`
- **Observers & filters**: codegen-free functional observers, incoming/outgoing call filters, request context propagation
- **TLS/mTLS**: Certificate-based silo and client encryption
- **Health checks & OpenTelemetry**: Production observability out of the box
- **Kubernetes clustering**: native K8s support with namespace and clustering configuration

## Next Steps

<CardGrid>
  <LinkCard title="Getting Started" href="/orleans-fsharp/getting-started/" description="Zero to working grain in 15 minutes." />
  <LinkCard title="How To" href="/orleans-fsharp/how-to/" description="Step-by-step distributed system tutorial." />
  <LinkCard title="Functional Grain Runtime" href="/orleans-fsharp/functional-grains/" description="The current grain authoring model, and the map off the deprecated one." />
  <LinkCard title="Event Sourcing" href="/orleans-fsharp/event-sourcing/" description="journaledGrainFor, confirmation semantics, and the classic model." />
  <LinkCard title="API Reference" href="/orleans-fsharp/api-reference/" description="Every public module, type, and function — functional surface first." />
  <LinkCard title="Calling from C#" href="/orleans-fsharp/calling-from-csharp/" description="The typed facade: awaited calls and await foreach from C#." />
  <LinkCard title="Comparison" href="/orleans-fsharp/comparison/" description="Orleans.FSharp vs C# Orleans vs Akkling vs Proto.Actor." />
  <LinkCard title="Security" href="/orleans-fsharp/security/" description="TLS, mTLS, call filters, secrets management." />
</CardGrid>
